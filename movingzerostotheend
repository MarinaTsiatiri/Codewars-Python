def move_zeros(lst):
    
    zeroes = []
    not_zeroes = [] 
    
    for num in lst:
        if num == 0:
            zeroes.append(num)
        else:
            not_zeroes.append(num)
            
    new_lst = not_zeroes + zeroes
    return new_lst
